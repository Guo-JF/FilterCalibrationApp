# 修复GitHub Actions构建失败

## 🔍 诊断步骤

### 方法1：查看GitHub上的错误日志

1. 访问：https://github.com/Guo-JF/FilterCalibrationApp/actions
2. 点击失败的workflow（红色❌）
3. 点击 "build" 任务
4. 查看哪一步失败了
5. 点开失败的步骤，查看完整错误信息

**常见错误及解决方案：**

#### 错误1：找不到 Android SDK
```
SDK location not found
```
**解决**：GitHub Actions会自动配置，通常不会出现此错误

#### 错误2：依赖下载失败
```
Could not resolve dependencies
Could not download ...
```
**解决**：网络问题，点击 "Re-run all jobs" 重新运行

#### 错误3：编译错误
```
Compilation error
Unresolved reference
```
**解决**：代码有问题，需要修复代码

#### 错误4：Gradle版本问题
```
Minimum supported Gradle version is ...
```
**解决**：更新 gradle/wrapper/gradle-wrapper.properties

#### 错误5：内存不足
```
OutOfMemoryError
```
**解决**：增加 gradle.properties 中的内存配置

---

### 方法2：本地测试构建

如果你想知道具体是什么问题，可以在本地测试：

```bash
cd /home/guo/滤波标定/FilterCalibrationApp
./本地测试构建.sh
```

这会显示详细的错误信息。

---

### 方法3：简化项目（快速修复）

如果构建一直失败，我可以帮你：

#### 选项A：简化依赖
移除一些可选依赖，只保留核心功能

#### 选项B：使用预构建的APK模板
基于一个已知可以构建的模板重新组织代码

#### 选项C：完全重建项目
从头创建一个更简单但功能完整的版本

---

## 🚀 快速解决方案

### 如果你想快速得到APK：

我建议使用**最小可行版本**：

1. 创建一个超级简化的Android项目
2. 只包含理论学习功能（最核心）
3. 去掉复杂的依赖
4. 保证能100%构建成功

这个版本会：
- ✅ 包含完整的10章理论内容
- ✅ 能正常安装运行
- ✅ 构建快速（2-3分钟）
- ❌ 但没有3D可视化（因为依赖复杂）
- ❌ 没有实战相机功能（需要相机权限）

**你要这个简化版本吗？**

---

## 📋 告诉我

请告诉我：

1. **GitHub Actions的具体错误信息**（访问Actions页面查看）
2. **你想要**：
   - A. 继续调试当前版本（可能需要多次尝试）
   - B. 使用简化版本（快速得到可用APK）
   - C. 暂时只看代码学习理论（不需要APK）

我会根据你的选择提供最佳方案！
