# GitHub 推送 403 错误解决方案

## 🔍 当前问题

错误：`Permission to Guo-JF/FilterCalibrationApp.git denied to Guo-JF`

## 可能的原因

### 1. Token权限不足
Token创建时**必须**勾选 `repo` 权限（包含所有子权限）

### 2. Token已过期或被撤销

### 3. 仓库设置问题

---

## ✅ 解决方案

### 方案1：重新创建Token（推荐）

1. **访问Token页面**
   ```
   https://github.com/settings/tokens
   ```

2. **删除旧Token**（如果有）
   - 找到名为 `FilterCalibrationApp` 的Token
   - 点击 "Delete"

3. **创建新Token**
   - 点击 "Generate new token" → "Generate new token (classic)"
   - **Note**: `FilterCalibrationApp-New`
   - **Expiration**: `90 days`
   - **勾选权限**（重要！）：
     ```
     ✅ repo (必须勾选，会自动展开子选项)
       ✅ repo:status
       ✅ repo_deployment
       ✅ public_repo
       ✅ repo:invite
       ✅ security_events
     ```
   - 滚动到底部，点击 "Generate token"
   - **立即复制保存新Token**

4. **使用新Token推送**
   ```bash
   cd /home/guo/滤波标定/FilterCalibrationApp
   
   # 使用新Token推送（替换为新Token）
   git push https://Guo-JF:新Token@github.com/Guo-JF/FilterCalibrationApp.git main
   ```

---

### 方案2：使用SSH方式（更简单，推荐）

如果你配置了SSH密钥，可以避免Token问题：

#### 2.1 检查是否有SSH密钥
```bash
ls -la ~/.ssh/id_*.pub
```

如果没有，生成一个：
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
# 一路回车（使用默认设置）
```

#### 2.2 添加SSH公钥到GitHub

```bash
# 显示公钥
cat ~/.ssh/id_ed25519.pub
```

复制输出，然后：
1. 访问：https://github.com/settings/keys
2. 点击 "New SSH key"
3. Title: `My Computer`
4. Key: 粘贴刚才复制的公钥
5. 点击 "Add SSH key"

#### 2.3 修改远程URL为SSH
```bash
cd /home/guo/滤波标定/FilterCalibrationApp
git remote set-url origin git@github.com:Guo-JF/FilterCalibrationApp.git
git push -u origin main
```

---

### 方案3：检查GitHub仓库设置

访问仓库设置页面：
```
https://github.com/Guo-JF/FilterCalibrationApp/settings
```

确认：
- Settings → General → Danger Zone
- 仓库没有被归档（Archived）
- 你是仓库的所有者

---

### 方案4：使用GitHub CLI（最简单）

```bash
# 安装GitHub CLI
sudo apt update
sudo apt install gh

# 登录（会自动处理认证）
gh auth login
# 选择：GitHub.com → HTTPS → Yes → Login with a web browser

# 推送代码
cd /home/guo/滤波标定/FilterCalibrationApp
git push -u origin main
```

---

## 🎯 我推荐的步骤

### 最简单的方法：使用GitHub CLI

```bash
# 1. 安装GitHub CLI
sudo apt update
sudo apt install gh

# 2. 登录
gh auth login

# 3. 推送
cd /home/guo/滤波标定/FilterCalibrationApp
git push -u origin main
```

### 或者：重新创建Token

1. 访问 https://github.com/settings/tokens
2. 删除旧Token，创建新Token
3. **确保勾选 repo 权限**
4. 使用新Token推送

---

## 📝 推送成功后

访问：https://github.com/Guo-JF/FilterCalibrationApp/actions

查看构建状态，等待5-10分钟后下载APK。

---

你想用哪个方案？我推荐方案4（GitHub CLI）最简单！
